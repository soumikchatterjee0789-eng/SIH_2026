fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
python-dotenv==1.0.1
email-validator==2.2.0
pytest==8.3.3
httpx==0.27.2

# Financial Analytics Engine (see README "Technical Approach"): pandas for
# grouping/aggregating income, expense, and transaction records; numpy for
# the mean/variability statistics behind income and expense stability
# scoring; scikit-learn for unusual-spending detection in
# services/spending_insights_service.py.
pandas>=2.2
numpy>=2.0
scikit-learn>=1.5

# Not installed by default - the app is DB-agnostic (SQLite for local/demo
# per PRD Section 6). Uncomment (or `pip install psycopg2-binary`) when
# pointing DATABASE_URL at PostgreSQL in staging/production.
# psycopg2-binary==2.9.9
